upload_max_filesize = 256M
post_max_size = 256M
memory_limit = 1024M
max_execution_time = 600
max_input_time = 600
default_charset = "UTF-8"
date.timezone = "Asia/Bangkok"

; Ensure proc_open / exec / shell_exec are enabled for Python worker invocations
disable_functions =
